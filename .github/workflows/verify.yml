name: Verify public evidence bundle

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify released arithmetic and boundaries
        run: python verify_bundle.py
      - name: Run regression tests
        run: python -m unittest discover -s tests -v
